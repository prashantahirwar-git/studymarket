# render.yaml — Render deployment config
# Commit this file to your GitHub repo root

services:
  - type: web
    name: studymarket
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --workers 2 --bind 0.0.0.0:$PORT --timeout 60 --keep-alive 5 --log-level info"
    envVars:
      - key: DATABASE_URL
        sync: false        # Set manually in Render dashboard
      - key: SECRET_KEY
        generateValue: true
      - key: RAZORPAY_KEY_ID
        sync: false
      - key: RAZORPAY_KEY_SECRET
        sync: false
      - key: SUPABASE_URL
        sync: false
      - key: SUPABASE_KEY
        sync: false
      - key: PYTHON_VERSION
        value: "3.11.0"
      - key: DB_POOL_MIN
        value: "2"
      - key: DB_POOL_MAX
        value: "8"
